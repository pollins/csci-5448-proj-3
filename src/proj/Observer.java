/**
 * 
 */
package proj;

/**
 * @author Empty observer interface to serve multiple types of observers.
 * The problem with the textbook's observer class is that a new interface has to be created for each set of parameters.
 *
 */
public interface Observer {

}
